# Smithery.ai configuration

startCommand:
  type: stdio
  
  # Configuration schema for your MCP server
  configSchema:
    type: object
    properties:
      logLevel:
        type: string
        enum: ["error", "warn", "info", "debug"]
        default: "info"
        description: "Logging level for the Think Tool MCP"
    required: []
  
  # Command function to start your MCP server
  commandFunction: |-
    (config) => ({
      "command": "deno",
      "args": ["run", "-A", "server.ts"],
      "env": {
        "DENO_LOG_LEVEL": config.logLevel || "info",
        "DENO_NO_UPDATE_CHECK": "1"
      }
    })

# Optional build configuration for Docker users
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."

